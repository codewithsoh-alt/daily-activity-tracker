server.port=8080

# Supabase PostgreSQL Configuration (Project: evvkuhqkrvzvkmoxqqxn, Region: ap-southeast-2 Sydney)
# IPv4 Connection Pooler (Transaction mode on port 6543 with prepareThreshold=0 for PgBouncer)
spring.datasource.url=${DB_URL:jdbc:postgresql://aws-0-ap-southeast-2.pooler.supabase.com:6543/postgres?sslmode=require&prepareThreshold=0}
spring.datasource.username=${DB_USERNAME:postgres.evvkuhqkrvzvkmoxqqxn}
spring.datasource.password=soham77890#
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA / Hibernate Configuration
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Jackson Configuration
spring.jackson.date-format=yyyy-MM-dd
spring.jackson.time-zone=UTC

